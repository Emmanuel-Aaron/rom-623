//Both use orientation to change altitude then resume back previous 
//oriantation by reading sensor

//Relative
elevate(altitude);

//Absolute
elevateTo(altitude);

//returns altitude
readAltitude();
